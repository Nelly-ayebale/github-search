export class Repository {
    constructor(public name: string, public html_url: string, public description: string, public languages_url: string, public collaborators_url: string, public created_at: Date) { }
}
